#include <stdio.h>
int ivert(char arreglo[], char arreglo2[];
{
    int i = 0;
    int d = sterlen(arreglo);
    while (i < tope)
    {
        arreglo2[i] = arreglo[d - 1];
        i++;
    }
    arreglo2[i] = '\0';
}

int main